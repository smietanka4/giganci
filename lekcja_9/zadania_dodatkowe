1) Utwórz listę zawierającą 5 dowolnych liczb całkowitych. 
Oblicz sumę drugiego i czwartego elementu tej listy i zapisz wynik do zmiennej suma.

Przykład wejścia:
liczby = [3, 7, 2, 9, 4]

2) Dla danej listy liczb całkowitych, utwórz nową listę zawierającą tylko liczby parzyste.

Przykład wejścia:
liczby = [1, 4, 5, 8, 11, 12]

3) Wczytuj kolejne elementy z listy, aż napotkasz liczbę ujemną. 
Zlicz, ile liczb zostało przetworzonych przed jej napotkaniem.

Przykład wejścia:
liczby = [2, 5, 3, 0, -1, 7]

Oczekiwany wynik:
4 -> 2,5,3,0

4) Dla danej listy imion, wypisz każde imię wraz z jego indeksem w formacie: 0: Ania, 1: Tomek, itd.

Przykład wejścia:
imiona = ["Ania", "Tomek", "Kasia"]

Oczekiwany wynik:
0: Ania, 1: Tomek, 2: "Kasia"

5) Dana jest macierz (lista list) liczb całkowitych. 
Oblicz sumę wszystkich elementów znajdujących się na przekątnej głównej tej macierzy.

Podpowiedź: Przekątna główna to elementy, które mają równe indeksy wierszy i kolumn 
(np. macierz[0][0], macierz[1][1], itd.).

macierz = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
]

6) Zadanie - dwie wersje: z wykorzystaniem ASCII lub funkcji
Odwracanie wielkości liter w przekazanym napisie. Napisz program, który wczytuje
napis od użytkownika, a następnie zamienia małe litery na duże, a duże na małe. Znaki
specjalne nie są zmieniane.
ASCII: Warto sprawdzić sobie tablicę ASCII. Dla uproszczenia rozważamy tylko
angielskie znaki: bez ‘ą’, ‘ę’, itd. Do odczytania numeru ASCII danego znaku należy
użyć funkcji ord(znak). Do zamiany liczby ASCII w znak użyj chr(liczba).
Bez ASCII: Wykorzystać funkcje: isupper(), islower(), lower(), upper()
